# Avalonia RichTextBox Port — Roadmap & Status

WPF `RichTextBox`/`FlowDocument`를 **순수 C# + Avalonia `TextLayout`**로 바닥부터 이식한다(PTS 없이 렌더·레이아웃·
히트테스트·선택·IME 직접 구현).

- 날짜별 상세 작업 로그(Phase·N·마일스톤·라운드2~32 원문) → [`docs/roadmap-archive.md`](docs/roadmap-archive.md)
- 릴리스별 변경 → [`CHANGELOG.md`](CHANGELOG.md) · 엔진 규칙 → [`CLAUDE.md`](CLAUDE.md)

이 파일은 **지금 상태 · 남은 일 · 아직 유효한 결정/교훈**만 둔다. 끝난 라운드의 경과는 아카이브로 옮긴다.

---

## ✅ 현재 상태 (2026-09-19)

- **게시**: `v1.2.1`(2026-09-08) — nuget.org. 이력: 0.9.0(07-12) → 1.0.0(07-31, API 동결) → 1.1.0(08-08) → 1.2.0/1.2.1(09-08).
- **미릴리스**: 09-09 이후 라운드(대체 텍스트 ~ 라운드32) 전부 `CHANGELOG.md` `[Unreleased]`. 공개 API **추가만 24건**
  (`PublicAPI.Unshipped.txt`, 제거·시그니처 변경 없음) → 다음은 **minor(1.3.0)**.
- **검증**: 빌드 0 warn · unit **1111** + render **41** 그린 · 3-OS CI(Windows/Linux/macOS) 그린.
- **의존성**: Avalonia **12.1** 이상(12.0.x는 Shift+Enter 2회로 `TextLayout` 무한 루프 — 엔진 버그라 상향이 유일한 수정).

**기능 (전부 동작·테스트 그린)**
- 인라인 서식(굵게/기울임/밑줄/취소선·글꼴·크기·색·형광펜·하이퍼링크), 문단(정렬·줄간격(HWP식 %)·들여쓰기·제목·목록·인용)
- **표**: 병합, 열/행 리사이즈, Tab 내비, 셀=블록 컨테이너(다중 문단·그림·구분선·**중첩 표**), **인라인 표**(HWP식 글자처럼),
  드래그 크기 지정 삽입, 셀 세로 정렬, F5 셀 블록 선택, 표·그림 끌어 옮기기(Ctrl=복사)
- **그림**: 블록/인라인, 모서리·가로·세로 손잡이, 교체·저장, 대체 텍스트, 표시 크기 디코드
- 찾기/바꾸기(찾기 막대·Ctrl+F/H·F3), undo/redo(64MB 예산), 우클릭 메뉴(HWP식), Word 표준 단축키
- 클립보드(내부 리치·CF_HTML·외부 HTML/RTF·이미지·Excel/TSV→표), **HTML/JSON/RTF/.flow** 입출력
- CJK **IME**, 페이지 보기, 인쇄, **벡터 PDF**(Skia + HarfBuzz 글꼴 추리기, 불가 시 래스터)
- 드롭인 `RichEditorView`(에디터+툴바+찾기 막대+상태바), `ToolbarLevel`, `IsReadOnly`+`Allow*`, 현지화(ko/en)

**완료된 마일스톤**: Phase 1~6 · P(페이지/인쇄) · N0~N6(NuGet) · A(셀 안 블록) · B(인라인 표) · G1(기하 워커 통합) ·
G2(문단 순회 워커 재귀화) · 1.0 준비 P1~P5(상호작용 테스트 인프라 · 렌더 픽셀 · 상호운용 · 성능 실측 · API 리뷰).

---

## 🎯 남은 일

**1. 다음 릴리스(1.3.0) 경계 결정** — 두 안 중 택일(미결):
- (a) 지금 쌓인 미릴리스분만으로 1.3.0 게시
- (b) 원래 계획한 호스트 API 3건(아래 백로그 위 3줄, 위험 순: 표 API → 단축키 공개 → 페이지 여백)까지 넣고 게시

게시 절차: `<Version>` → Unshipped를 Shipped로 이관 → CHANGELOG `[Unreleased]` → 버전 → README 배지(en/ko) → PR로 3-OS CI →
`v1.3.0` 태그 push(**사람이 실행**, Trusted Publishing) → **nuget.org 패키지 페이지 육안 확인**.

**2. 실기 확인 대기** (기록상 확인 완료가 남지 않은 것)
- 라운드21 · 표 복사/잘라내기: 뷰어에서 표 우클릭 → 복사 → Word/HWP 붙여넣기 · 경계 클릭/우클릭 후 Ctrl+C(편집·뷰어) ·
  인라인 표·셀 안 표 경계의 커서·클릭·우클릭 · 표를 잡고 잘라내기/Delete → Ctrl+Z 복원 · 인라인 표를 글줄 중간에 붙여넣기
- 라운드23 · 끌어 옮기기: 테두리·그림 끌기 · Ctrl 복사와 "+" · 자기 셀로 끌 때 금지 커서 · 클릭만 하면 전과 같은지

**3. 포트(WinUIRichEditor)로 넘길 것** (포트 쪽 반영 여부 확인 필요)
- 그림 손잡이 vs 열 경계 우선순위: **선택된 그림 손잡이 우선**(여기 방식)으로 포트를 맞춘다 (사용자 결정 2026-09-19)
- 테스트 `CtrlU_AtALinksEnd_LeavesTheLinkAlone`(라운드19에서 여기만 추가)

**알려진 제한 (의도적으로 둠)**
- RTF 중첩 표의 열 너비는 가져올 때 기본값(`\cellx`가 무시되는 props 그룹 안에 있다).
- RTF는 비대칭: 인라인 표·대체 텍스트는 RTF 자리가 없음(인라인 표는 `{\*\arinline}`로 자체 왕복만 복원).
- 붙여넣기 세 경로(앱 내부·RTF·HTML)의 **배선**은 헤드리스에 실제 클립보드가 없어 테스트 밖(적응 로직은 `InsertHtml`로 검증).
- mac/Linux는 best-effort(3-OS CI 그린, 실기 동작은 리포트 주도).

---

## 📐 아직 유효한 결정

| 결정 | 날짜 |
|---|---|
| 손상 파일(JSON/`.flow`/`document.json` 없는 zip)은 **예외** — 빈 문서로 읽지 않는다(호스트가 원본을 덮어쓰게 됨) | 07-31, 09-12 |
| 상호운용 검증은 **리포트 주도** — 선제 전수 파기 중단. 재현 도구는 [`tools/rtfgen/`](tools/rtfgen/README.md)(Word COM 읽기 전용 하네스, HWP는 자동화 불가 → 사람) | 08-08 |
| 줄 간격 = **글자 크기 × HWP 비율**, 기본 160%, 문단 아래 여백 기본 0(그림·표는 10). 포트도 동일 | 09-15~16 |
| 표를 통째로 잡은 상태(전체 선택·블록 캐럿)의 Delete/잘라내기 = **표 삭제**. 일부 셀 블록은 셀만 비움 | 09-13 |
| 셀 블록 = 선택 두 끝에서 파생(모드 플래그 없음). 한 셀 블록은 `_cellBlockMark`. 포트와 통일 | 09-13 |
| 제목 적용 시 run 크기: **여기는 본문 기본값으로 초기화**, 포트는 직접 지정 크기 유지 — 알려진 분기 | 09-12 |
| WinUI의 "게이트 추출"(순수 함수화)은 백포트하지 않는다 — 여기는 `InteractionHost`로 실입력 구동이 이미 가능 | 08-06 |
| CI 퍼즈 시드는 20(병합 결함을 8/20이 잡음), 큰 시드(5000)는 손으로 | 09-07 |
| 벡터 PDF는 Skia PDF(여러 OS). 포트는 별도로 Windows "Print to PDF" 경로 | 09-12 |

---

## 🧠 누적 교훈 (반복해서 확인된 것만)

- **자체 왕복은 writer·reader가 공유하는 방언을 못 본다.** Word/HWP/브라우저 측정만이 잡았다(RTF 테두리 없음, `\clmgf` 병합, 용지).
  육안보다 측정 — 브라우저 `getBoundingClientRect`, Word 객체 모델.
- **포매터는 전부 붙여넣기로 닿는 신뢰 불가 입력이다.** 정상 왕복 말고 깨진·악성 입력을 넣을 것. 누적형 결함은 **반복 왕복(고정점)**으로만 보인다.
- **생성 쪽이 만들지 않는 형태는 조용히 0% 커버다.** 불변식을 늘리는 것과 그 불변식이 실행되게 하는 것은 다른 일(퍼즈에 병합이 없었다).
- **반증(수정을 되돌려 테스트가 빨개지는지)을 매번 한다.** 폴백이 "틀린 이유로 맞는 답"을 주는 공허한 테스트가 여러 번 잡혔다.
- **새 축을 열 땐 하네스부터 의심하라.** 안 끝나는/죽는 테스트는 재실행 말고 **프로세스를 소유해서 계측**(콘솔 앱 + `dotnet-stack`).
- `InvalidateMeasure` 누락 주장 전 체인 확인: `ResetCaretBlink()`→`NotifyStatus()`→`InvalidateMeasure()`, `PushUndo()`는 레이아웃 캐시를 버린다
  (외부 감사 8건 오진의 원인).
- 프로세스 전역 브러시/펜(정적 필드·속성 기본값)은 `Immutable*` — 스레드 귀속 크래시. 전역 스캔 테스트가 지킨다.
- **헤드리스 테스트 함정**: 그림 디코드가 필요하면 `[AvaloniaFact]`(아니면 그림이 조용히 안 들어옴) · 내부 클립보드는 static(복사 전 비울 것) ·
  메뉴 Click만 일으키면 메뉴가 열린 채 남음 · 임포터는 64px 미만을 인라인으로 · 반증 복원을 `Copy-Item`으로 하면 옛 수정 시각 → 증분 빌드가 교란 DLL 사용.
- 게시 후 nuget.org 패키지 페이지를 눈으로 본다 — README raw HTML이 태그 그대로 찍힌 걸 5주간 몰랐다(1.2.1).

---

## 🔵 백로그 (착수 미정)

- **표 행/열 조작 공개 API** — `TableInsertRow`/`TableDeleteColumn` 등이 private. 모델 직접 조작은 undo·캐시 무효화를 건너뛴다. 추가성.
- **`RichEditorShortcuts` 공개** — 자체 툴바를 만드는 호스트가 제스처 표를 읽을 수 없다. 추가성.
- **페이지 여백 API**
- **문서 암호화(`.flow`)** — 설계 완료(07-31): 매직 헤더 + AES-256-GCM + PBKDF2-SHA256 600k, 암호화 파일은 zip이 아니게.
  무의존성(PBKDF2 600k = 124ms). **미결**: Argon2id용 의존성 1개 수용 여부.
- 단락 경계 넘는 Find(줄바꿈 포함 검색어만, 실사용 극저) · HWP/XLS(구) 붙여넣기 · blockquote/중첩 목록 깊이 정밀화
- 인라인 객체 공통 추상(`InlineObject`) — 세 번째 인라인 타입이 실제로 생기면
- 의도적 보류(측정상 저가치): 델타 Undo · `ComputePageBreaks` 캐시 · HTML `HasBlockOrMedia` O(n²) · `ReplaceAll` O(N²) · G1 P2(BlockBox 캐싱)

---

## 📌 작업 규칙

- 진행 상황·보류 항목은 **이 파일**을 먼저 확인하고, 작업 후 갱신한다. 라운드 경과는 끝나면 아카이브로.
- 각 단계는 독립 출하 + 테스트 그린 유지. GUI 경로(포인터·포커스·키 조합)는 데모 실기 확인 — 라운드3 결함 24건 중 11건이 여기서만 나왔다.
- 비자명한 엔진 규칙은 [`CLAUDE.md`](CLAUDE.md) "비자명한 핵심 규칙" 참조.
