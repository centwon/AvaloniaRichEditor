# tools/rtfgen

**솔루션(`AvaloniaRichEditor.slnx`)에 일부러 넣지 않았다** — 손으로 돌리는 개발 도구라 여기를 건드려도 CI는 영향받지 않는다.

쓰는 법·함정·방침(리포트 주도)은 전부 [`README.md`](README.md)에 있다. 손대기 전에 그 "함정" 절부터 읽을 것 —
특히 **Word COM은 읽기 전용으로만**(쓰기는 헤드리스에서 멈춘다), **`.ps1`은 UTF-8 BOM**.
